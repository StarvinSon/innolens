name: PostSpaces

method: post
path: /api/spaces/spaces

authentication:
  type: bearer

requestBody:
  contentType: application/json
  schema:
    type: object
    required:
      - fileId
    additionalProperties: false
    properties:
      fileId:
        type: string
