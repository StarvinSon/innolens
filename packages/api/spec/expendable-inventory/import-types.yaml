name: ImportTypes

method: post
path: /api/expendable-inventories/types

authentication:
  type: bearer

requestBody:
  contentType: application/json
  schema:
    type: object
    required:
      - fileId
    additionalProperties: false
    properties:
      fileId:
        type: string
