name: PostTypes

method: post
path: /api/reusable-inventories/types

authentication:
  type: bearer

requestBody:
  contentType: application/json
  schema:
    type: object
    required:
      - fileId
    additionalProperties: false
    properties:
      fileId:
        type: string
